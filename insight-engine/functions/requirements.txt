# Function dependencies
functions-framework==3.*
google-cloud-storage>=2.0.0
cloudevents==1.3.0
structlog>=21.5.0

# Required for local development and dependency resolution
# This line assumes the 'insight_engine' package is in the parent directory.
# In a real deployment, you would package this differently.
-e ../